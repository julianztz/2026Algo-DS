# 贪心算法快速参考卡片

## 🎯 何时用Greedy？

### ✅ 适用条件
1. **贪心选择性质**：局部最优 → 全局最优
2. **最优子结构**：问题最优解包含子问题最优解
3. **不需要回溯**：一旦选择就确定

### ❌ 不适用情况
- 局部最优 ≠ 全局最优（如Coin Change）
- 需要比较所有可能性
- 需要回溯

---

## 📊 Greedy vs DP 对比

| 特征 | Greedy | DP |
|------|--------|-----|
| **选择时机** | 立即选择局部最优 | 计算完所有子问题后选择 |
| **时间复杂度** | O(n) 或 O(n log n) | O(n²) 或更高 |
| **空间复杂度** | 通常 O(1) 或 O(n) | 需要存储子问题 |
| **子问题** | 只需要一个最优子问题 | 需要所有子问题 |

---

## 🔧 四大模板

### 模板1：区间/范围类
```python
# 1. 排序
items.sort(key=lambda x: x[1])  # 按结束时间/关键值

# 2. 贪心选择
result = []
current = items[0]
for item in items[1:]:
    if 满足条件:
        result.append(item)
        current = item
```

**典型问题**：活动选择、区间合并、无重叠区间

---

### 模板2：最值选择类（堆）
```python
import heapq

heap = []
for item in items:
    heapq.heappush(heap, item)
    if len(heap) > k:
        heapq.heappop(heap)  # 保持堆大小
```

**典型问题**：Top K、合并K个链表、第K大元素

---

### 模板3：累积/更新类
```python
current = 0
max_value = 0
count = 0

for i, num in enumerate(nums):
    current += num  # 或 max(current, num)
    
    if 满足条件:
        count += 1
        current = 重置值
    
    max_value = max(max_value, current)
```

**典型问题**：跳跃游戏、加油站、买卖股票

---

### 模板4：排序+选择类
```python
# 1. 排序（关键：选择合适的排序规则）
items.sort(key=lambda x: 排序规则)

# 2. 贪心匹配
i = j = 0
while i < len(A) and j < len(B):
    if 满足匹配条件:
        count += 1
        i += 1
    j += 1
```

**典型问题**：分发饼干、任务分配、字典序问题

---

## 🚀 解题步骤

1. **识别特征**：优化问题 + 最优子结构？
2. **设计策略**：每一步的选择标准？
3. **实现算法**：初始化 → 遍历 → 选择 → 更新
4. **验证正确性**：用反例验证（不行就用DP）

---

## ⚠️ 常见陷阱

### 陷阱1：Coin Change不能用Greedy
```python
# ❌ 错误：Greedy
coins = [1, 3, 4], amount = 6
Greedy: 4+1+1 = 3个硬币 ❌
正确: 3+3 = 2个硬币 ✅

# ✅ 正确：DP
需要计算所有可能的组合
```

### 陷阱2：需要证明贪心选择性质
- 不能只凭直觉
- 用反例验证
- 如果Greedy不确定，用DP保证正确性

---

## 📝 经典问题分类

| 类型 | 典型问题 | 模板 |
|------|---------|------|
| **区间问题** | 活动选择、无重叠区间 | 模板1 |
| **分配问题** | 分发饼干、任务调度 | 模板4 |
| **最值问题** | 跳跃游戏、加油站 | 模板3 |
| **堆问题** | Top K、合并链表 | 模板2 |

---

## 💡 判断技巧

```
问题：求最优解
    ↓
有最优子结构？
    ↓ 是
局部最优 → 全局最优？
    ↓ 是          ↓ 否
用Greedy      用DP
```

**快速判断**：
- 需要O(n)时间 → 尝试Greedy
- 需要比较所有可能 → 用DP
- 用反例验证 → 如果Greedy出错就用DP

---

## 🎓 总结

- **Greedy优势**：时间复杂度低，代码简洁
- **Greedy劣势**：适用面窄，需要证明正确性
- **建议**：不确定时先用DP，再尝试优化为Greedy











